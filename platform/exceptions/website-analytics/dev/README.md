# Policy exceptions — website-analytics / dev

Topics, subjects and connectors that the platform team has agreed may break a
policy. Files here are applied with an **AdminToken**, which bypasses
`ResourcePolicy` validation.

There are no dev exceptions right now. See
[`../prod/high-partition-topic.yml`](../prod/high-partition-topic.yml) for a
worked example.
